apiVersion: v1
kind: ConfigMap
metadata:
  name: customer-churn-config
  namespace: ml-prod
  labels:
    app: customer-churn-api
    environment: production
data:
  PORT: "8000"
  ENVIRONMENT: "production"
  LOG_LEVEL: "INFO"
  APP_NAME: "customer-churn-api"
